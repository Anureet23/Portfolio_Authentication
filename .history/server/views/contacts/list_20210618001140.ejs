<!--Header Partial Included -->
<% include ../partials/header.ejs %>

<!--Top Navbar Partial Included -->
<% include ../partials/navbar.ejs %>

<!--Main Content-->
<main class="container">
    <div class="row">
        <div class="col-md-offset-3 col-md-10">
            <h1><%= title %> List</h1>
            <a href="/contacts-list/add" class="btn btn-primary" role="button">
                <i class="fas fa-plus-circle"></i>
                Add a Contact</a>
            <br><br>
            <table class="table table-bordered table-striped table-hover">
                <!--Header Row-->
                <thead class="table-dark">
                <tr>
                    <th class="text-center col-2">Name</th>
                    <th class="text-center col-2">Contact Number</th>
                    <th class="text-center col-1">Email Address</th>
                    <th class="text-center col-1"> </th>
                    <th class="text-center col-1"> </th>
                </tr>
               </thead>
               <tbody>
                <!--Repeatble Template Row-->
 
                  <% for(let count =0; count < ContactsList.length; count ++) {%>
                    <tr>
                        <td class="text-center"><%= ContactsList[count].contactname %></td>
                        <td class="text-center"><%= ContactsList[count].contactnumber %></td>
                        <td class="text-center"><%= ContactsList[count].emailaddress %></td>
                        <td class="text-center col-1">
                            <a href="/contacts-list/edit/<%= ContactsList[count]._id %>" class="btn btn-primary btn-sm">
                          <i class="fas fa-pencil-alt"></i> Edit</a>
                        </td>
                        <td class="text-center col-1">
                            <a href="/contacts-list/delete/<%= ContactsList[count]._id %>" class="btn btn-danger btn-sm">
                            <i class="fas fa-trash-alt"></i> Delete</a>
                        </td>
                    </tr>
                  <%}%>
            </table>
        </div>
    </div>
</main>



 <!--Bottom Navbar Partial Included -->
 <% include ../partials/bottomnavbar.ejs %> 

<!--Footer Partial Included -->
 <% include ../partials/footer.ejs %>